import { IssueSeverity } from 'fork-ts-checker-webpack-plugin/lib/issue/IssueSeverity';
import { IssueLocation } from 'fork-ts-checker-webpack-plugin/lib/issue/IssueLocation';
interface Issue {
    origin: string;
    severity: IssueSeverity;
    code: string;
    message: string;
    file?: string;
    location?: IssueLocation;
}
declare function isIssue(value: unknown): value is Issue;
declare function deduplicateAndSortIssues(issues: Issue[]): Issue[];
export { Issue, isIssue, deduplicateAndSortIssues };
