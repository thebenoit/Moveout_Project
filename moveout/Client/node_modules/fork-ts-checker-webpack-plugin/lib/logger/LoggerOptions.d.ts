import { LoggerType } from 'fork-ts-checker-webpack-plugin/lib/logger/LoggerFactory';
import Logger from 'fork-ts-checker-webpack-plugin/lib/logger/Logger';
declare type LoggerOptions = {
    infrastructure?: LoggerType | Logger;
    issues?: LoggerType | Logger;
    devServer?: boolean;
};
export default LoggerOptions;
